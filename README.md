🚀 Инструкции за стартиране на проекта FitTrack

Този проект се състои от два основни компонента, които работят заедно:

FitTrack Main App (Монолитно приложение) – Основното уеб приложение.

BadgeService (Микросървис) – REST услуга за управление на значки и постижения.

За да функционира системата правилно, е необходимо да следвате стъпките по-долу.

🛠️ 1. Предварителни изисквания

Уверете се, че имате инсталирани следните инструменти:

Java JDK: Версия 17.

Maven: За изграждане на проектите.

Сървър за база данни: MySQL.

⚙️ 2. Настройка на базата данни

Проектът използва база данни за съхранение на потребители, цели и тренировки.

Стартирайте вашия MySQL сървър (напр. чрез MySQL Workbench или команден ред).

Създайте нова, празна база данни с име: fittrack_db (или името, зададено в application.properties).

Бележка: Не е необходимо да създавате таблици ръчно. При първото стартиране Hibernate автоматично ще създаде необходимата схема (ddl-auto=update).

Забележка за проверяващите: Конфигурационните файлове (application.properties) съдържат необходимите данни за връзка (потребителско име и парола), за да се улесни стартирането. Моля, уверете се, че локалните ви настройки за MySQL съвпадат с тези във файла (напр. порт 3306) или ги променете временно.

▶️ 3. Стартиране на приложенията

Важно е да спазвате реда на стартиране, за да може основното приложение да се свърже успешно с микросървиса.

Стъпка 1: Стартиране на BadgeService (Микросървис)

Този сървис трябва да работи, за да може FitTrack да изпраща заявки за значки.

Отворете микросървиса BadgeService и стартирайте класа BadgeServiceApplication

Изчакайте, докато видите съобщение, че приложението е стартирало (обикновено на порт 8081).

Стъпка 2: Стартиране на FitTrack (Основно приложение)

След като BadgeService работи:

Отворете FitTrack Main App и стартирайте класа FitTrackApplication.

Изчакайте успешното стартиране на приложението (обикновено на порт 8080).

🌐 4. Достъп до приложението

След като и двата сървиса работят:

Отворете уеб браузър.

Навигирайте до: http://localhost:8080

Можете да се регистрирате като нов потребител и да започнете да използвате системата:

Добавяне на цели.

Записване на тренировки.

Следене на прогреса и автоматично получаване на значки (чрез комуникация с BadgeService).
